# This file makes 'app.api.v1' a Python package.
# API routes for version 1 will be defined in modules within this package.

# Example: If using Flask Blueprints or FastAPI APIRouters
# from flask import Blueprint
# v1_blueprint = Blueprint('v1', __name__, url_prefix='/api/v1')
# from . import dashboard_endpoints # To register routes defined in dashboard_endpoints.py

# Or for FastAPI:
# from fastapi import APIRouter
# router = APIRouter(prefix="/api/v1")
# from . import dashboard_endpoints
# router.include_router(dashboard_endpoints.router)
